# For Linux (if Docker socket is non-default)
# docker.host=unix:///var/run/docker.sock

# For Windows/Mac (usually auto-detected)
docker.client.strategy=org.testcontainers.dockerclient.DockerMachineClientProviderStrategy

# Optional: Enable reuse to speed up tests
testcontainers.reuse.enable=true